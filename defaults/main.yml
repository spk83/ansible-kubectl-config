---
kubectl_config_user: k8s
kubectl_config_user_token: JKQ8n5stwH5UaTjfWNH4qli2bharF5wM

kubectl_config_install_dir : /usr/local/bin

# Following vars should be same as the one used to setup cluster
# Reference role - andrewrothstein.kubernetes-cluster
k8s_cluster_name: mykubecluster

k8s_secured: True
k8s_src_pki_dir: /pki-dir
k8s_pki_cert_suffix: '.pem'

k8s_master_group_name: kubernetes-master
  
k8s_network_iface: eth0
k8s_apiserver_secure_port: 6443

#k8s_apiserver_secure_lb: https://lb.kube-api.xyz.com:6443