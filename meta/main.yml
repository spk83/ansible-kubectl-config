---
galaxy_info:
  author: Vishal Shah
  description: Configures kubectl to connect to your k8s cluster
  company: BlackRock
  license:
    - MIT
  min_ansible_version: 2.0
  platforms:
    - name: EL
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
    - name: Alpine
      versions:
        - all

  galaxy_tags:
    - kubectl
    - k8s

dependencies:
  - role: andrewrothstein.kubectl
    version: v1.0.5
    kubectl_install_dir: '{{kubectl_config_install_dir}}'
